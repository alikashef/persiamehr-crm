services:
  frontend:
    build:
      context: ./front-end
      dockerfile: Dockerfile.runtime
    restart: unless-stopped
    environment:
      NEXT_PUBLIC_API_URL: ${NEXT_PUBLIC_API_URL:-/api/}
    ports:
      - "127.0.0.1:3001:3000"
